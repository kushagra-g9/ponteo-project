apiVersion: v1
kind: Service
metadata:
  name: ponteo-project
  namespace: ponteo
  labels:
    app.kubernetes.io/name: ponteo-project
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: ponteo
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ponteo-project
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
